pkgname=`dpkg-parsechangelog -S source`
version=`dpkg-parsechangelog -S version`
pkg_pattern="${pkgname}_${version}"
package="${pkg_pattern}_all.deb"
pkgpath="$HOME/$package"
pkgfiles="$HOME/${pkg_pattern}*"

red=$(tput setaf 4)
green=$(tput setaf 2)
reset=$(tput sgr0)
toend=$(tput hpa $(tput cols))$(tput cub 6)